name: Build and Release

on:
    push:
        branches: [ "main" ]
        tags: [ "v*.*.*", "v*" ]
    pull_request:
        branches: [ "main" ]

permissions:
    contents: write

jobs:
    build:
        name: Build Cross-Platform
        runs-on: ubuntu-latest
        strategy:
            matrix:
                goos: [ linux, darwin, freebsd ]
                goarch: [ amd64, arm64 ]

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Go
              uses: actions/setup-go@v5
              with:
                  go-version: "1.21"

            - name: Build Binary
              env:
                  GOOS: ${{ matrix.goos }}
                  GOARCH: ${{ matrix.goarch }}
              run: |
                  mkdir -p build
                  go build -o build/wpvul-${{ matrix.goos }}-${{ matrix.goarch }} main.go

            - name: Upload Artifact
              uses: actions/upload-artifact@v4
              with:
                  name: wpvul-${{ matrix.goos }}-${{ matrix.goarch }}
                  path: build/wpvul-${{ matrix.goos }}-${{ matrix.goarch }}

    release:
        name: Create GitHub Release
        needs: build
        if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Download All Artifacts
              uses: actions/download-artifact@v4
              with:
                  path: build-artifacts

            - name: Bump version and push tag
              id: tag_version
              if: github.ref == 'refs/heads/main'
              uses: mathieudutour/github-tag-action@v6.2
              with:
                  github_token: ${{ secrets.GITHUB_TOKEN }}
                  default_bump: patch

            - name: Prepare Files for Release
              run: |
                  mkdir release-binaries
                  # Github actions artifact downloader creates a folder per artifact
                  for dir in build-artifacts/*; do
                    mv "$dir"/* release-binaries/
                  done

            - name: Publish Release to GitHub
              uses: softprops/action-gh-release@v2
              with:
                  tag_name: ${{ steps.tag_version.outputs.new_tag || github.ref_name }}
                  files: release-binaries/*
                  generate_release_notes: true
